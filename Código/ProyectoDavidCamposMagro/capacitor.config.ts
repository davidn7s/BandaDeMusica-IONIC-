import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Banda de Música San Sebastián',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
